# Prerventech Website

A clean, responsive starter website for Prerventech.

## Project Structure

- `index.html` - Main page structure
- `styles.css` - Styling and layout
- `script.js` - Basic interactivity
- `assets/` - Images, logos, and media files

## Run Locally

Open `index.html` directly in your browser, or use a local server:

```powershell
cd prerventech-website
python -m http.server 8080
```

Then visit `http://localhost:8080`.

## Next Steps

- Replace placeholder text with your real business content
- Add your logo and brand images in `assets/`
- Connect the contact form to your backend service
